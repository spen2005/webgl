// Define the light object
const light = {
    // Light position
    position: { x: 0, y: 0, z: 0 },
    
    // Light color
    color: { r: 1.0, g: 1.0, b: 1.0 },

    // Light intensity
    intensity: 1.0
};

// Export the light object so that other modules can access it if needed
export default light;
